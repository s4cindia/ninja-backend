Enhance remediation tasks with full issue details

  The remediation plan tasks only show "Accessibility issue detected" instead of the original audit issue details.

  Fix: When creating remediation tasks, copy ALL fields from the original audit issue:

  // In remediation.service.ts - when creating tasks from audit issues

  async createRemediationPlan(jobId: string) {
    const auditResult = await this.getAuditResult(jobId);

    const tasks = auditResult.issues.map(issue => ({
      jobId,
      issueCode: issue.code,
      severity: issue.severity,
      type: this.isAutoFixable(issue.code) ? 'auto' : 'manual',
      status: 'pending',

      // ADD THESE FIELDS - copy from original issue
      description: issue.message,           // "Publications must declare..."
      location: issue.location,             // "EPUB/wasteland.opf"
      filePath: issue.filePath,             // For View in Context
      selector: issue.selector,             // CSS selector for highlighting
      wcagCriteria: issue.wcagCriteria,     // ["1.3.1", "4.1.2"]
      source: issue.source,                 // "ACE" | "EPUBCheck" | "JS Auditor"
      html: issue.html,                     // Snippet for context
      remediation: this.getRemediationGuidance(issue.code), // Fix steps
    }));

    await this.prisma.remediationTask.createMany({ data: tasks });
  }

  Update Prisma schema if needed:

  model RemediationTask {
    id            String   @id @default(uuid())
    jobId         String
    issueCode     String
    severity      String
    type          String   // 'auto' | 'manual'
    status        String   // 'pending' | 'completed'

    // Add these fields
    description   String   // Full issue message
    filePath      String?  // For View in Context
    selector      String?  // CSS selector
    wcagCriteria  String[] // WCAG references
    source        String?  // ACE, EPUBCheck, JS Auditor
    html          String?  // Code snippet

    // ... existing fields
  }

  Update the GET endpoint to return full details:

  // Return all fields in the remediation plan response
  return {
    tasks: tasks.map(task => ({
      ...task,
      description: task.description,
      wcagCriteria: task.wcagCriteria,
      source: task.source,
      // Include remediation guidance
      remediation: this.getRemediationGuidance(task.issueCode),
    })),
  };